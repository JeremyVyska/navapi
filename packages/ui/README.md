# `@navapi/ui`

Secure loopback-only web interface used by `navapi ui`.

The package is a thin browser face over `@navapi/core`. It shares the same profiles, OS-keychain secrets, metadata caches, and active company as the CLI, MCP server, and VS Code extension.
